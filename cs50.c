#include <stdio.h>
int size(char b[i]);
int main(void)
{
	char a;
	int l;
	printf("Enter the character: ");
	scanf("%c", &a);
	l = size(a);
	printf(" the size is; %d", l);
}
int size(char b[i])
{
	int i;
	{
		for (i = 0; i <= b[i]; i++)
	}
	return i;
}
